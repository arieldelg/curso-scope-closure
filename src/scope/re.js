/* var firstName; // undefined
firstName = 'Ariel';
console.log(firstName);

var lastName = 'David'; // declarar / asignar
lastName = 'Ana'; // reasignar
console.log(lastName);

var secondName = 'David'; // declarando / asignando
var secondName = 'Ana'; // reasignado
console.log(secondName);

// let
let fruit = 'banana' // declarar y asignar
fruit = 'apple'; // reasignar
console.log(fruit);

let fruit = 'ariel'; // aunque se pueda reasignar, let no se puede volver a declarar por segunda vez
console.log(fruit);

 // const

 const animal = 'dog'; // declarar y asigna
 animal = 'cat'; // con const no se puede reasignar
 const animal = 'perico'; // con const no se puede declarar
 console.log(animal);

// si se puede ya que hace referencia al arreglo
 const carros = []; 
 carros.push('ariel');
 console.log(carros);

// si se puede ya que hace referencia la arreglo
 carros.pop();
 console.log(carros);

 //no se puede ya que hace referencia a la variable const
 const carros = ['ariel'];
 console.log(carros);
  */